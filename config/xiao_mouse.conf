# Enable mouse / pointer HID support
CONFIG_ZMK_MOUSE=y

# Enable USB HID (works when plugged in via USB-C)
CONFIG_ZMK_USB=y

# BLE is enabled by default on the XIAO nRF52840 board config.
# Uncomment to disable BLE if you only want USB:
# CONFIG_BT=n
